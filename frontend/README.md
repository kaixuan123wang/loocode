# Loocode Frontend

Vite+ and pnpm monorepo shell for the Loocode frontend.

## Workspace

- `apps/web` - Vue 3 application shell.
- `packages/shared` - shared workspace package placeholder.

## Commands

```bash
vp install
pnpm dev
pnpm build
```
